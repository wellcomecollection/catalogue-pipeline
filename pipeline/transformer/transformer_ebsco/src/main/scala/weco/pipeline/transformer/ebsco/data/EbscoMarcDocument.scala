package weco.pipeline.transformer.ebsco.data

/*
 * Represents a MARC XML Document as sent by EBSCO
 *
 * A Document consists of a <collection> element containing multiple <record> elements
 * */
case class EbscoMarcDocument()
