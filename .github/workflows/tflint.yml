name: "Terraform: tflint"

on:
  pull_request:
    paths:
      - "**/*.tf"
      - "**/.terraform.lock.hcl"
      - ".tflint.hcl"
      - ".github/workflows/tflint.yml"
  push:
    branches:
      - main
    paths:
      - "**/*.tf"
      - "**/.terraform.lock.hcl"
      - ".tflint.hcl"
      - ".github/workflows/tflint.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  tflint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: terraform-linters/setup-tflint@v4
        with:
          tflint_version: v0.64.0

      - name: Run tflint on all Terraform roots
        shell: bash
        # tflint --init downloads rulesets from GitHub; unauthenticated calls
        # share the runner IP's rate limit and fail with a 403.
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: builds/run_tflint.sh
