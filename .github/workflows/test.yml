# Build deployment artifact 
name: "Run tests"

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Setup CI
        run: |
          ./scripts/ci-setup.sh

      - name: Setup CI
        run: |
          ./scripts/test.sh