ROOT = $(shell git rev-parse --show-toplevel)
include $(ROOT)/makefiles/functions.Makefile

STACK_ROOT  = common

SBT_APPS 	 =
SBT_SSM_APPS =

ECR_BASE_URI =
REGISTRY_ID  =

SBT_DOCKER_LIBRARIES    =
SBT_NO_DOCKER_LIBRARIES = display internal_model

PYTHON_APPS     =
PYTHON_SSM_APPS =
LAMBDAS 	    =

TF_NAME =
TF_PATH =

TF_IS_PUBLIC_FACING = false

$(val $(call stack_setup))
